{
  "name": "contact-backend",
  "version": "1.0.0",
  "description": "Simple Express backend to send contact form emails via SMTP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "nodemailer": "^6.9.3"
  }
}
